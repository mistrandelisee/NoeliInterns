import { LightningElement, api, wire, track } from 'lwc';

export default class Rh_displayDataEvent extends LightningElement {
        data = {
                Name : 'Will',
                City:'Yaoundé',
                Phone:'458885554'

        };
}